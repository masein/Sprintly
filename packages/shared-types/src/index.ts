// Re-export the auto-generated types once the backend emits an OpenAPI spec.
// Until M1 phase 2 wires that pipeline up, this file is intentionally empty.
//
// After `just gen-types`:
//   export * from "./generated";

export type Placeholder = never;
